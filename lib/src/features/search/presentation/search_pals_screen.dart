import 'package:flutter/material.dart';

class SearchPalsScreen extends StatelessWidget {
  const SearchPalsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Find a PushPal')),
      body: const Center(child: Text('Search functionality coming soon!')),
    );
  }
}
